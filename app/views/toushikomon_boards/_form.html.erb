<%= form_for(@toushikomon_board) do |f| %>
  <% if @toushikomon_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@toushikomon_board.errors.count, "error") %> prohibited this toushikomon_board from being saved:</h2>

      <ul>
      <% @toushikomon_board.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :toushikomon_id %><br />
    <%= f.number_field :toushikomon_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
